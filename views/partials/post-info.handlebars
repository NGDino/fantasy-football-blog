<li>
    <article class="post">
      <div class="title">
        <a href="a href="/post/{{id}}" >{{title}}</a>
        <p>{{post_text}}</p>
      </div>
      <div class="meta">
        By {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
    </article>
</li>