<div class="row">
  <div class="col s12">
    <div class="card-panel indigo lighten-4">
      <ol class="post-list">
        {{#each posts as |post|}}
        {{> post-info post}}
        <div class="divider grey darken-3"></div>
        {{/each}}
      </ol>
    </div>
  </div>
</div>